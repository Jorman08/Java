// Una clase en Java es una plantilla o modelo que define las características (atributos) y comportamientos (métodos) que tendrán los objetos que se creen a partir de ella.
// Es la base de la Programación Orientada a Objetos (POO).

// Características:

// Contiene atributos (variables).
// Contiene métodos (funciones asociadas).
// Puede tener constructores para inicializar los objetos.

// ¿Qué es un objeto?

// Un objeto es una instancia de una clase.
// Cuando creamos un objeto, estamos creando una copia en memoria de la estructura definida por la clase.

// Características:

// Cada objeto tiene sus propios valores en los atributos.
// Se crean con la palabra clave new.